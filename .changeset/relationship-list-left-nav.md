---
"@mj-biz-apps/common-ng": patch
---

Relationship list fills leftover left-nav height without parking its header mid-column. Type lookup is UUID-normalized so links still render when ID casing differs, and the empty state says there are no relationships instead of a blank panel.
