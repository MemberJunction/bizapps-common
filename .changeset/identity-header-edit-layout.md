---
"@mj-biz-apps/common-ng": patch
---

Person and Organization identity headers stack edit fields as labeled columns. Each field is wrapped so mj-form-field's display:contents cannot leak into a parent grid. Two-across only when the hero is at least 52rem wide; URLs and description still span the full row.
