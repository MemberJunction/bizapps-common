---
"@mj-biz-apps/common-ng": patch
---

Restyle Person/Org identity headers to match the payment card: compact surface, badge row, and a metric strip. PhotoURL / LogoURL already replace initials when set.
